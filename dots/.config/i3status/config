general {
  output_format = "lemonbar"
  colors = true
  separator = "|"
  interval = 5
}

order += "load"
order += "volume master"

load {
  format = "[ load: %1min, %5min, %15min ]"
}

volume master {
  format = "vol: %volume"
  format_muted = "vol: muted"
  device = "default"
}