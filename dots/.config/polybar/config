[colors]
background = ${xrdb:background:#282A36}
foreground = ${xrdb:foreground:#F8F8F2}

black = ${xrdb:color0}
black-alt = ${xrdb:color8}
red = ${xrdb:color1}
red-alt = ${xrdb:color9}
green = ${xrdb:color2}
green-alt = ${xrdb:color10}
yellow = ${xrdb:color3}
yellow-alt = ${xrdb:color11}
blue = ${xrdb:color4}
blue-alt = ${xrdb:color12}
magenta = ${xrdb:color5}
magenta-alt = ${xrdb:color13}
cyan = ${xrdb:color6}
cyan-alt = ${xrdb:color14}
white = ${xrdb:color7}
white-alt = ${xrdb:color15}


# Main bar configuration
[bar/x]
width = 100%
height = 28
offset-x = 0
offset-y = 0
radius = 0.0
fixed-center = true
background = ${colors.background}
foreground = ${colors.foreground}
padding-left = 1
padding-right = 1
module-margin-left = 1
module-margin-right = 1

font-0 = EnvyCodeR:size=10;1
font-1 = Font\ Awesome\ 5\ Free:size=15;0
font-2 = Wuncon\ Siji:size=15;0
font-3 = Hack\ Nerd\ Font:size=16;2

#font-1 = "Hack Nerd Font;size=22;antialias=true;4"
#font-1 = MaterialIcons:pixelsize=16;antialias=false;6

modules-left = menu-actions volume bspwm
modules-center = xwindow
modules-right = wireless-network battery cpu mem date

tray-position = right
tray-padding = 2

wm-restack = bspwm

line-size = 4
line-color = ${colors.cyan}
border-color = ${colors.black}
border-size = 0
separator = |

# BSPWM module configuration
[module/bspwm]
type = internal/bspwm
format-prefix = bspwm
format-prefix-foreground = ${colors.cyan}
format-prefix-padding = 2

format = <label-state> <label-mode>

label-monocle = [mon]
label-tiled = [tiled]
label-fullscreen = [fs]
label-floating = [float]
label-locked = [locked]
label-sticky = [sticky]
label-private = [priv]

ws-icon-0 = term;
ws-icon-1 = emacs;
ws-icon-2 = chrome;
ws-icon-3 = spotify;
ws-icon-4 = web;hi
ws-icon-5 = chat;
ws-icon-default = hi

label-focused = %icon%
label-focused-font = 2
#label-focused-background = ${colors.black}
#label-focused-underline = ${colors.red-alt}
label-focused-padding = 1
label-focused-foreground = ${colors.green}

label-occupied = %icon%
#%name%
label-occupied-foreground = ${colors.foreground}
#blue}
label-occupied-padding = 1

label-urgent = %icon%
label-urgent-background = ${colors.red}
label-urgent-padding = 1

label-empty = %icon%
label-empty-foreground = ${colors.black-alt}
#foreground}
label-empty-padding = 1
# 

[module/volume]
type = internal/volume
format-volume = <label-volume> <bar-volume>
format-volume-prefix = 
label-volume = 
label-volume-foreground = ${colors.black-alt}
#${colors.foreground}
format-muted = <label-muted>
format-muted-prefix = 
format-muted-prefix-foreground = ${colors.black-alt}
format-muted-foreground = ${colors.black-alt}
label-muted = [muted]
label-muted-width = 15
bar-volume-width = 10
bar-volume-foreground-0 = ${colors.green}
bar-volume-foreground-1 = ${colors.green}
bar-volume-foreground-2 = ${colors.green}
bar-volume-foreground-3 = ${colors.green}
bar-volume-foreground-4 = ${colors.green}
bar-volume-foreground-5 = ${colors.yellow}
bar-volume-foreground-6 = ${colors.red}
bar-volume-gradient = false
bar-volume-indicator = /
bar-volume-indicator-font = 0
bar-volume-fill = /
bar-volume-fill-font = 0
bar-volume-empty = " "
bar-volume-empty-font = 0
bar-volume-empty-foreground = ${xrdb:color0}


[module/date]
type = internal/date
interval = 5.0
date = 
date-alt = " %Y-%m-%d"
time = %H:%M
time-alt = %H:%M:%S
format-prefix = time
format-prefix-foreground = ${colors.black-alt}
#${colors.foreground}
label = %date% %time%

[module/xwindow]
type = internal/xwindow

[module/menu-actions]
type = custom/menu

expand-right = true

border-size = 2
border-color = ${colors.white}
border-left = ${colors.white}

format-padding = 2
format-prefix =  Arch Linux
format-prefix-foreground = ${colors.cyan}

label-open = "abry"
label-open-margin = 2
label-open-padding = 2
label-open-underline = ${colors.blue}

label-close = close
label-close-margin-left = 2
label-close-padding = 2
label-close-underline = ${colors.blue}

label-separator = " | "
label-separator-padding = 0
label-separator-margin = 0

menu-0-0 = power
menu-0-0-underline = ${colors.red}
menu-0-0-exec = menu-open-1
menu-0-1 = actions
menu-0-1-underline = ${colors.green}
menu-0-1-exec = menu-open-2

menu-1-0 = logout
menu-1-0-exec = pkill X > /dev/null 2>&1
menu-1-0-underline = ${colors.red}
menu-1-1 = shutdown
menu-1-1-exec = sudo systemctl poweroff
menu-1-1-underline = ${colors.red}
menu-1-2 = reboot
menu-1-2-exec = sudo systemctl reboot
menu-1-2-underline = ${colors.red}

menu-2-0 = rofi
menu-2-0-exec = rofi -show run
menu-2-0-underline = ${colors.green}
menu-2-1 = scrot
menu-2-1-exec = scrot &
menu-2-1-underline = ${colors.green}

[module/wired-network]
type = internal/network

[module/wireless-network]
type = internal/network
interface = wlp107s0
interval = 3.0
ping-interval = 5

format-connected = <ramp-signal> <label-connected>
label-connected = %essid% %downspeed:9%
ramp-signal-0 = 

[module/cpu]
type = internal/cpu
interval = 1
format = <label> <ramp-load>
format-prefix = "cpu "
format-prefix-foreground = ${colors.black-alt}
label = %percentage%%
ramp-load-0 = ▁
ramp-load-1 = ▂
ramp-load-2 = ▃
ramp-load-3 = ▄
ramp-load-4 = ▅
ramp-load-5 = ▆
ramp-load-6 = ▇
ramp-load-7 = █

[module/mem]
type = internal/memory
interval = 5
format = <label>
format-prefix = "mem "
format-prefix-foreground = ${colors.black-alt}
label = %percentage_used%%

[module/battery]
type = internal/battery
battery = BAT1
adapter = ADP1

time-format = %H:%M
format-charging = <animation-charging> <label-charging>
format-discharging = <ramp-capacity> <label-discharging>

label-charging = charging %percentage%%
label-discharging = discharging %percentage%%

ramp-capacity-0 = 
ramp-capacity-1 = 
ramp-capacity-2 = 
ramp-capacity-3 = 
ramp-capacity-4 = 

animation-charging-0 = 

[settings]
format-foreground = ${colors.foreground}
#format-underline = ${colors.background}
#format-overline = ${colors.black}
format-padding = 0 #2
format-margin = 2