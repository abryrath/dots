#
# ~/.bash_profile
#

# Disable beeps
# setterm -blength 0

# bash config
[[ -f ~/.bashrc ]] && . ~/.bashrc

# bash completion
# [[ -f /usr/local/share/bash-completion/bash_completion ]] &&  . /usr/local/share/bash-completion/bash_completion
